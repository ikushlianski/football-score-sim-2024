{
  "name": "tournaments",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tournaments/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/tournaments",
        "main": "tournaments/src/main.ts",
        "tsConfig": "tournaments/tsconfig.app.json",
        "assets": ["tournaments/src/assets"],
        "webpackConfig": "tournaments/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "tournaments:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "tournaments:build:development"
        },
        "production": {
          "buildTarget": "tournaments:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "tournaments/jest.config.ts"
      }
    }
  },
  "tags": []
}
